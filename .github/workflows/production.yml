name: Production workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  # Linux-job:
  #   name: Inject application.properties
  #   runs-on: ubuntu-latest

  #   Steps:
  #     -uses: actions/checkout@v2
  #       run: mvn spring-boot:run -Dspring-boot.run.arguments="--spring.data.mongodb.uri =${{secrets.MONGODB_URI}}"
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      -uses: actions/checkout@v2
        run: mvn spring-boot:run -Dspring-boot.run.arguments="--spring.data.mongodb.uri =${{secrets.MONGODB_URI}}"
      - name: Deploy to heroku using an action
        uses: AkhileshNS/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
          heroku_app_name: "restaurant-at-unimelb-api" 
          heroku_email: "yihang1@student.unimelb.edu.au"
          appdir: "IT"